<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
    http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.23.xsd">

    <changeSet id="001-create-user-table" author="dominik">
        <createTable tableName="app_user">
            <column name="id" type="bigserial">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="username" type="varchar(100)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="password_hash" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="role" type="varchar(20)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="001-user-role-check" author="dominik">
        <sql>
            ALTER TABLE "app_user"
                ADD CONSTRAINT chk_user_role
                    CHECK (role IN ('ADMIN','STUDENT','PROF'));
        </sql>
    </changeSet>

    <changeSet id="002-create-refresh-token-table" author="dominik">
        <createTable tableName="refresh_token">
            <column name="id" type="bigserial">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="bigint">
                <constraints nullable="false"
                             referencedTableName="app_user"
                             referencedColumnNames="id"
                             foreignKeyName="fk_refresh_token_user"/>
            </column>

            <column name="token_hash" type="varchar(255)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="expires_at" type="timestamp with time zone">
                <constraints nullable="false"/>
            </column>

            <column name="revoked_at" type="timestamp with time zone"/>

            <column name="replaced_by_id" type="bigint"/>
        </createTable>
    </changeSet>
</databaseChangeLog>