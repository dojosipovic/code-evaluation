export enum TaskQueryParamEnum {
    PAGE = 'page',
    SIZE = 'size',
    SEARCH = 'search',
    SORT_BY = 'sortBy',
    SORT_DIRECTION = 'sortDir',
    STATUS = 'status',
    ENABLED = 'enabled',
    SHARED = 'shared',
    EXCLUDE_CURRENT_USER = 'excludeCurrentUser'
}
