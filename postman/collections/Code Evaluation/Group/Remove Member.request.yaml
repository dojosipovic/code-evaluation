$kind: http-request
url: http://localhost:8080/api/groups/:groupId/members/:userId
method: DELETE
pathVariables:
  groupId: "1"
  userId: "3"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
settings: {}
order: 3000
